# Homebrew tap for Editor

Mac install for [koughen/Editor](https://github.com/koughen/Editor):

```sh
brew install --cask koughen/editor/editor
```

The cask is updated automatically by the Editor release workflow on every tag.
